"""Deterministic categorisation by weighted lexicon.

This package replaces the trained classifier. Every decision it makes traces to
a term an administrator can read, and the same input always produces the same
output — no artifacts, no training, no inference.
"""
